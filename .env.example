# Cloud server environment variables (set in Railway dashboard)
USER_PASSWORD=change-me-password

# Local bridge environment variables (set on your machine)
SERVER_URL=wss://your-app.up.railway.app/bridge
