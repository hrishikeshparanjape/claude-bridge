# Cloud server environment variables (set these in Railway/Render dashboard)
BRIDGE_SECRET=change-me-bridge-secret
USER_PASSWORD=change-me-password

# Local bridge environment variables (set these on your machine)
SERVER_URL=wss://your-app.up.railway.app/bridge
BRIDGE_SECRET=change-me-bridge-secret
